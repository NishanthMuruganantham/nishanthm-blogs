---
import { slugify } from '../lib/utils';

interface Props {
  id?: string;
  title: string;
  description: string;
  excerpt?: string;
  date: string | Date;
  category: string;
  tags: string[];
  slug: string;
  cover?: string;
  readingTime?: string;
}

const { title, description, excerpt, date, category, tags = [], slug, cover, readingTime } = Astro.props;

const dateStr = typeof date === 'string' ? date : new Date(date).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric'
});
---

<div class="blog-card" data-post-slug={slug}>
  <div class="blog-card-content">
    <a href={`/categories/${slugify(category)}/`} class="category-badge">{category}</a>
    <h2 class="card-title">
      <a href={`/blogs/${slug}/`}>{title}</a>
    </h2>
    <p class="card-excerpt">{description || excerpt}</p>
    
    <div class="card-metadata">
      <time datetime={typeof date === 'string' ? date : date.toISOString()} class="date">{dateStr}</time>
      <span class="read-time">{readingTime || '3 min read'}</span>
    </div>
    
    {tags.length > 0 && (
      <div class="card-tags">
        {tags.map(tag => (
          <a href={`/tags/${slugify(tag)}/`} class="tag">#{tag}</a>
        ))}
      </div>
    )}
    
    <a href={`/blogs/${slug}/`} class="read-more" aria-hidden="true">Read more &rarr;</a>
  </div>
  
  <a href={`/blogs/${slug}/`} class="blog-card-image" tabindex="-1" aria-hidden="true">
    {cover ? (
      <img src={cover} alt={title} loading="lazy" decoding="async" />
    ) : (
      <div class="image-fallback"></div>
    )}
  </a>
</div>
