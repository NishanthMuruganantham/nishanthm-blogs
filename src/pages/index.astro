---
import BaseLayout from "../layouts/BaseLayout.astro";
import Button from "../components/ui/Button.astro";
import AgentNetwork from "../components/AgentNetwork.astro";
import PostCard from "../components/PostCard.astro";
import FeaturedPost from "../components/FeaturedPost.astro";
import { getCollection } from "astro:content";
import { getReadingTime } from "../lib/utils";

// Fetch all non-draft posts
const allPosts = await getCollection("blog", ({ data }) => !data.draft);

// Sort newest first
const sortedPosts = allPosts.sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf(),
);

// Get the specific featured post
const featuredPost = sortedPosts.find((p) => p.data.featured);

// Get reading time for featured post if it exists
const featuredPostWithMeta = featuredPost
  ? {
      ...featuredPost,
      readingTime: featuredPost.body
        ? getReadingTime(featuredPost.body)
        : undefined,
    }
  : null;

// Get latest posts (excluding the featured one)
const latestPosts = sortedPosts
  .filter((p) => p.slug !== featuredPost?.slug)
  .slice(0, 3);

const latestPostsWithMeta = latestPosts.map((post) => ({
  ...post,
  readingTime: post.body ? getReadingTime(post.body) : undefined,
}));

// Count posts per category
const categories = [
  { name: "Backend", emoji: "⚙️", color: "backend" },
  { name: "Frontend", emoji: "🎨", color: "frontend" },
  { name: "Tools", emoji: "🛠️", color: "tools" },
  { name: "Android", emoji: "📱", color: "android" },
  { name: "Startup", emoji: "🚀", color: "startup" },
];

const categoryCounts = categories.map((cat) => ({
  ...cat,
  count: allPosts.filter((p) => p.data.category === cat.name).length,
}));
---

<BaseLayout
  title="Home"
  description="Personal blog by Nishanth M, documenting engineering challenges in Django, AWS, and Agentic AI."
  includeNetwork={false}
>
  <!-- ===================== HERO ===================== -->
  <section class="hero hero-gradient hero--space" id="hero-section">
    <!-- Animated agent network canvas -->
    <AgentNetwork intensity="hero" />

    <div class="hero__content" id="hero-content">
      <p class="hero__eyebrow anim-fade" style="--delay: 0">
        Developer · Builder · Writer
      </p>

      <h1 class="hero__heading anim-fade" style="--delay: 1">
        <span class="hero-accent-text">Welcome to My Engineering Notebook</span
        ><br />
        <span class="hero-accent-text"></span>
      </h1>

      <div class="hero__sub anim-fade" style="--delay: 2">
        <p>
          This is my engineering notebook for documenting the journey from
          backend systems to agentic AI.
        </p>
        <p>
          I architect with Django and Python on AWS, automate with N8N,
          implement RAG systems, and design autonomous agents. Currently
          exploring Claude, Google AI Studio, and MCPs.
        </p>
        <p>
          This blog captures what works: architecture patterns, infrastructure
          decisions, AI integrations, and the documented path of shipping
          intelligent systems.
        </p>
      </div>

      <div class="hero__cta anim-fade" style="--delay: 3">
        <Button as="a" href="/blog" variant="ghost" id="cta-blog"
          >Read my Blogs</Button
        >
        <Button as="a" href="/about" variant="ghost" id="cta-about"
          >About Me</Button
        >
      </div>
    </div>
  </section>

  <section class="section" id="latest-writing">
    <div class="section__inner">
      {
        featuredPostWithMeta && (
          <FeaturedPost
            post={featuredPostWithMeta}
            readingTime={featuredPostWithMeta.readingTime}
          />
        )
      }

      <h2 class="section__title">Latest Writing</h2>
      <div class="posts-grid">
        {
          latestPostsWithMeta.map((post) => (
            <PostCard
              title={post.data.title}
              description={post.data.description}
              date={post.data.date}
              category={post.data.category}
              tags={post.data.tags}
              slug={post.slug}
              readingTime={post.readingTime}
              cover={post.data.cover}
            />
          ))
        }
      </div>
      <div class="section__cta">
        <a href="/blog" class="btn-ghost" id="cta-all-posts">View all posts →</a
        >
      </div>
    </div>
  </section>

  <!-- =============== CATEGORIES =============== -->
  <section class="section" id="categories-section">
    <div class="section__inner">
      <h2 class="section__title">Browse by Topic</h2>
      <div class="categories-grid">
        {
          categoryCounts.map((cat) => (
            <a
              href="/categories/"
              class={`category-card category-card--${cat.color}`}
              id={`cat-${cat.color}`}
            >
              <span class="category-card__emoji">{cat.emoji}</span>
              <span class="category-card__name">{cat.name}</span>
              <span class="category-card__count">
                {cat.count} {cat.count === 1 ? "post" : "posts"}
              </span>
            </a>
          ))
        }
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* =====================================================
     HERO SECTION
     ===================================================== */
  .hero {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 90vh;
    padding: 6rem 1.5rem 6rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  /* Smooth bottom fade — blends hero into page content seamlessly */
  .hero::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 160px;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      var(--color-bg) 100%
    );
    z-index: 1;
    pointer-events: none;
  }

  /* Deep-space radial gradient — DARK mode default */
  .hero--space {
    background: radial-gradient(ellipse at bottom, #1b2735 0%, #090a0f 100%);
  }

  /* Light mode override: soft sky-dawn gradient */
  :global(html:not(.dark)) .hero--space {
    background: radial-gradient(
      ellipse at 50% 110%,
      #c7d7f8 0%,
      #dde8ff 40%,
      #f0f4ff 100%
    );
  }

  /* Light mode: tint the glow blob to a softer indigo */
  :global(html:not(.dark)) .hero__glow-blob {
    background: radial-gradient(
      circle,
      rgba(124, 106, 247, 0.15) 0%,
      transparent 70%
    );
    opacity: 0.6;
  }

  /* ── Agent Network Canvas ── */
  .hero__network {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
  }

  /* ── Light mode font contrast overrides ── */

  /* Eyebrow: deep indigo so it pops on lavender */
  :global(html:not(.dark)) .hero__eyebrow {
    color: #3730a3;
  }

  /* Heading text: dark indigo for light theme */
  :global(html:not(.dark)) .hero__heading .hero-accent-text {
    color: #1e1b4b;
  }

  /* Default (dark mode) hero text: bright accent */
  .hero-accent-text {
    color: var(--color-accent-bright);
  }

  /* Subtext: near-black for maximum legibility */
  :global(html:not(.dark)) .hero__sub {
    color: #1e293b;
  }

  /* Respect reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .hero__network {
      display: none;
    }
  }

  .hero__content {
    max-width: 720px;
    position: relative;
    z-index: 2;
  }

  .hero__eyebrow {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    color: var(--color-teal-bright);
    margin-bottom: 1.5rem;
  }

  .hero__heading {
    font-family: var(--font-serif-display);
    font-size: clamp(2.5rem, 6vw, 4.25rem);
    font-weight: 900;
    line-height: 1.15;
    margin-bottom: 1.75rem;
  }

  .hero__sub {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    color: var(--color-text-secondary);
    font-size: 1.1rem;
    line-height: 1.85;
    letter-spacing: 0.01em;
    max-width: 580px;
    margin: 0 auto 2.25rem;
  }

  .hero__sub p {
    margin: 0;
  }

  .hero__cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Staggered fade-in animation */
  .anim-fade {
    opacity: 0;
    filter: blur(8px);
    transform: translateY(24px);
    animation: fadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: calc(var(--delay) * 0.15s);
  }

  @keyframes fadeUp {
    to {
      opacity: 1;
      filter: blur(0);
      transform: translateY(0);
    }
  }

  /* =====================================================
     SHARED SECTION LAYOUT
     ===================================================== */
  .section {
    padding: var(--space-section) 1.5rem;
  }

  .section__inner {
    max-width: var(--wide-width);
    margin: 0 auto;
  }

  .section__title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(2rem, 4vw, 2.5rem);
    color: var(--color-text-primary);
    margin-bottom: 2.5rem;
  }

  .section__cta {
    text-align: center;
    margin-top: 2.5rem;
  }

  /* Mobile-only line break utility */
  .hide-mobile {
    display: inline;
  }
  @media (max-width: 640px) {
    .hide-mobile {
      display: none;
    }
  }

  /* =====================================================
     POSTS GRID
     ===================================================== */
  .posts-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 640px) {
    .posts-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .posts-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* =====================================================
     CATEGORIES GRID
     ===================================================== */
  .categories-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  @media (min-width: 768px) {
    .categories-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .categories-grid {
      grid-template-columns: repeat(5, 1fr);
    }
  }

  .category-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding: 1.75rem 1rem;
    border-radius: 0.75rem;
    border: 1px solid var(--color-border-subtle);
    text-decoration: none;
    transition: all var(--transition-base);
    cursor: pointer;
  }

  .category-card:hover {
    transform: translateY(-3px);
    border-color: transparent;
  }

  /* Category-specific backgrounds (10% opacity) */
  .category-card--backend {
    background: rgba(249, 115, 22, 0.06);
  }
  .category-card--frontend {
    background: rgba(59, 130, 246, 0.06);
  }
  .category-card--tools {
    background: rgba(168, 85, 247, 0.06);
  }
  .category-card--android {
    background: rgba(34, 197, 94, 0.06);
  }
  .category-card--startup {
    background: rgba(236, 72, 153, 0.06);
  }

  /* Hover fills */
  .category-card--backend:hover {
    background: rgba(249, 115, 22, 0.18);
    box-shadow: 0 8px 24px rgba(249, 115, 22, 0.12);
  }
  .category-card--frontend:hover {
    background: rgba(59, 130, 246, 0.18);
    box-shadow: 0 8px 24px rgba(59, 130, 246, 0.12);
  }
  .category-card--tools:hover {
    background: rgba(168, 85, 247, 0.18);
    box-shadow: 0 8px 24px rgba(168, 85, 247, 0.12);
  }
  .category-card--android:hover {
    background: rgba(34, 197, 94, 0.18);
    box-shadow: 0 8px 24px rgba(34, 197, 94, 0.12);
  }
  .category-card--startup:hover {
    background: rgba(236, 72, 153, 0.18);
    box-shadow: 0 8px 24px rgba(236, 72, 153, 0.12);
  }

  .category-card__emoji {
    font-size: 1.75rem;
    line-height: 1;
  }

  .category-card__name {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1rem;
    color: var(--color-text-primary);
  }

  .category-card--backend .category-card__name {
    color: var(--color-backend);
  }
  .category-card--frontend .category-card__name {
    color: var(--color-frontend);
  }
  .category-card--tools .category-card__name {
    color: var(--color-tools);
  }
  .category-card--android .category-card__name {
    color: var(--color-android);
  }
  .category-card--startup .category-card__name {
    color: var(--color-startup);
  }

  .category-card__count {
    font-family: var(--font-display);
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  /* Code snippet card */
  .code-snippet-card {
    background: var(--code-bg);
    border: 1px solid var(--code-border);
    border-radius: 0.75rem;
    overflow: hidden;
    font-size: 0.85rem;
    box-shadow: 0 8px 32px var(--glass-shadow);
  }

  .code-snippet__header {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.75rem 1rem;
    background: var(--code-lang-bg);
    border-bottom: 1px solid var(--code-border);
  }

  .code-snippet__dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .code-snippet__dot--red {
    background: #ff5f57;
  }
  .code-snippet__dot--yellow {
    background: #febc2e;
  }
  .code-snippet__dot--green {
    background: #28c840;
  }

  .code-snippet__filename {
    margin-left: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .code-snippet__body {
    padding: 1.25rem 1rem;
    margin: 0;
    overflow-x: auto;
    font-family: var(--font-mono);
    line-height: 1.7;
    color: var(--color-text-primary);
    background: transparent;
    border: none;
  }

  .code-snippet__body code {
    font-family: inherit;
    background: transparent;
    padding: 0;
    color: inherit;
  }

  .code-kw {
    color: var(--color-accent-bright);
  }
  .code-cls {
    color: #fbbf24;
  }
  .code-str {
    color: #4ade80;
  }
  .code-fn {
    color: #60a5fa;
  }

  /* =====================================================
     NEWSLETTER
     ===================================================== */
  .section--newsletter {
    border-top: 1px solid var(--color-border-subtle);
    text-align: center;
    position: relative;
  }

  .section--newsletter::before {
    content: "";
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 3px;
    background: linear-gradient(90deg, var(--color-accent), var(--color-teal));
    border-radius: 2px;
  }

  .newsletter-inner {
    max-width: 520px;
  }

  .newsletter__title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    color: var(--color-text-primary);
    margin-bottom: 0.75rem;
  }

  .newsletter__sub {
    color: var(--color-text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .newsletter__form {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .newsletter__input {
    flex: 1;
    padding: 0.75rem 1rem;
    background: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    color: var(--color-text-primary);
    font-family: var(--font-body);
    font-size: 0.95rem;
    transition: border-color var(--transition-fast);
    outline: none;
  }

  .newsletter__input::placeholder {
    color: var(--color-text-muted);
  }

  .newsletter__input:focus {
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px var(--color-accent-glow);
  }

  .newsletter__btn {
    white-space: nowrap;
  }

  .newsletter__note {
    color: var(--color-text-muted);
    font-size: 0.75rem;
  }

  @media (max-width: 480px) {
    .newsletter__form {
      flex-direction: column;
    }
  }
</style>

<script>
  const prefersReducedMotion = window.matchMedia(
    "(prefers-reduced-motion: reduce)",
  );

  // ── Scroll parallax for hero content ──
  const heroContent = document.getElementById("hero-content");
  if (heroContent && !prefersReducedMotion.matches) {
    window.addEventListener("scroll", () => {
      if (window.scrollY < window.innerHeight) {
        requestAnimationFrame(() => {
          const yPos = window.scrollY * 0.3;
          const opacity = 1 - (window.scrollY / window.innerHeight) * 1.5;
          heroContent.style.transform = `translateY(${yPos}px)`;
          heroContent.style.opacity = String(opacity > 0 ? opacity : 0);
        });
      }
    });
  }
</script>
